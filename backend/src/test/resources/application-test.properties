# Test profile: the datasource comes from the Testcontainers @ServiceConnection.
# Flyway runs the V1 (schema) and V2 (demo data) migrations inside the container.
spring.jpa.hibernate.ddl-auto=validate
logging.level.org.testcontainers=INFO
